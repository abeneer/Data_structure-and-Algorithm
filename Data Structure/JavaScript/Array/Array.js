const numbers = [1, 10, 100, 1000, 10000]  // Declare and initialize array
console.log(numbers)

 // How to acces an element from the array

console.log("first element:", numbers[0]);
console.log("last element:", numbers[numbers.length - 1]);

// Number of elements in the array 
console.log("number of elements in Array:", numbers.length);

// Update the elements in the array 
numbers[2] = 101; 
console.log("After updating 3rd element:", numbers.join(" "))




